version: '3'

services:
  broker-service:
    build:
      context: ./broker-service
      dockerfile: broker-service.Dockerfile
    restart: always
    ports:
      - "8070:8070"
    deploy:
      mode: replicated
      replicas: 1
